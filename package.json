{
  "name": "professional-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix site run dev",
    "build": "npm --prefix site run build",
    "check": "npm --prefix site run check",
    "test": "npm --prefix library/thermostat-mobile/example test",
    "verify": "npm run check && npm test && npm run build && node scripts/verify.mjs && node scripts/verify-seo.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "playwright": "^1.55.0"
  }
}
